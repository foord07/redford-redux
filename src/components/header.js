import header from '../assets/images/header.png';

const Header = () => {
    return (
        <div className="header-box">
            <img src={header} alt="UBS Header"/>
        </div>
    );
}
export default Header
